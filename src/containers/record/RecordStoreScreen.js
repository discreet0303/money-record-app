import React, {useState} from 'react';
import _ from 'lodash';
import moment from 'moment';

import {
  View,
  Text,
  StyleSheet,
  Button,
  TouchableOpacity,
  TextInput,
  KeyboardAvoidingView,
} from 'react-native';

const RecordStoreScreen = () => {
  return (
    <View>
      <Text>123</Text>
    </View>
  );
};

export default RecordStoreScreen;
